@page "/states/edit/{Id:int}"

<h3>Editar Estado</h3>

@if (State == null)
{
    <Loading />
}
else
{
    <Form @ref=Form
          State="State"
          OnSubmit="_Update"
          ReturnAction="ReturnAction" />
}
